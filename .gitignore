# Datasets must never be committed: ~1.2 GB, over GitHub's file-size limits,
# and derived from CUB-200-2011 + Places, so not ours to redistribute.
data/
datasets/
waterbird*/
celeba*/
*.tar.gz
*.tgz
*.zip

# Cached features and results (regenerable)
*.npz

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# OS
.DS_Store

# Trained ERM weights (regenerable, ~100 MB)
*.pt
*.pth
